# Employees Feature

Employees is now wired as the first implemented HR feature slice.

## Implemented

- Employee CRUD page via the shared resource view.
- Backend-backed list, show, create, update, and delete flows.
- Division and position relation selectors.
- Feature bridge for dashboard routing.

## Entry Point

- `src/features/employees/pages/EmployeesPage.tsx`

## Notes

- The feature currently leans on the shared CRUD shell.
- Next polish can add search, richer detail cards, and onboarding actions.
