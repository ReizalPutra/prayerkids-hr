# Auth Feature

This folder will contain authentication workflows.

## Planned Scope

- Login flow.
- Session helpers and auth state utilities.
- Forgot password or reset password flows if supported by backend.
- Token cleanup and access guards.

## Starter Structure

```text
src/features/auth/
  pages/
  components/
  hooks/
  services/
  types/
  utils/
```

## Next Step

Move the login-specific UI and auth state helpers here once the feature grows beyond the shared hook layer.
